Single vs. multi (pool)
=======================

.. warning::

   **This page no longer has a subject.** The single/pool selector was removed:
   the pool always runs and sizes itself, so there is nothing to choose between.
   What used to be here — where a user lives, how the pool grows, what is shared
   — is now in :doc:`the-pool`.

   The file is kept only so an existing link does not break, and it is out of the
   navigation. It can be deleted.

:doc:`the-pool`
